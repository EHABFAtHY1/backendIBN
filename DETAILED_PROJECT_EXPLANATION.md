# شرح مفصل شامل للمشروع - Employee Management System

## 📚 فهرس المحتويات
1. [نظام الأدوار والصلاحيات](#نظام-الأدوار-والصلاحيات)
2. [هيكل قاعدة البيانات](#هيكل-قاعدة-البيانات)
3. [API Endpoints](#api-endpoints)
4. [Workflow العملية](#workflow-العملية)
5. [معالجة الأخطاء](#معالجة-الأخطاء)

---

## نظام الأدوار والصلاحيات

### 🔑 1. Admin (المسؤول)

**الصلاحيات الكاملة:**

```
✅ الوظائف:
├─ إنشاء موظفين جدد
├─ عرض جميع الموظفين
├─ تعديل بيانات أي موظف
├─ حذف موظفين
├─ إسناد المشاريع
└─ عرض البيانات الشخصية (راتب، رقم قومي، إلخ)

✅ API Endpoints:
├─ POST   /api/employees          (إنشاء)
├─ GET    /api/employees          (عرض الكل)
├─ PUT    /api/employees/:id      (تعديل)
├─ DELETE /api/employees/:id      (حذف)
└─ PUT    /api/employees/:id/projects (مشاريع)
```

**مثال: Admin ينشئ موظف**
```bash
curl -X POST http://localhost:5000/api/employees \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "أحمد",
    "lastName": "محمد",
    "email": "ahmed@example.com",
    "password": "SecurePass123!",
    "phoneNumber": "0501234567",
    "employeeId": "EMP001",
    "position": "engineer",
    "department": "إنشاءات",
    "hireDate": "2024-01-15"
  }'
```

---

### 👤 2. Employee (الموظف)

**الصلاحيات المحدودة:**

```
✅ يمكنه:
├─ تسجيل الدخول بحسابه
├─ عرض ملفه الشخصي الكامل
├─ عرض دليل الموظفين العام
└─ عرض البيانات الأساسية لزملائه

❌ لا يمكنه:
├─ إنشاء موظفين
├─ تعديل أي بيانات
├─ حذف حسابات
└─ الوصول لبيانات زملائه الشخصية

✅ API Endpoints:
├─ GET /api/employees/me         (ملفي الشخصي)
├─ GET /api/employees            (دليل الموظفين)
└─ GET /api/employees/:id        (موظف واحد)
```

**مثال: Employee يعرض ملفه**
```bash
curl -X GET http://localhost:5000/api/employees/me \
  -H "Authorization: Bearer <employee_token>"

# الرد يتضمن:
{
  "success": true,
  "data": {
    "firstName": "أحمد",
    "lastName": "محمد",
    "ssn": "123456789",           # ✅ مرئي
    "salary": 5000,               # ✅ مرئي
    "address": "الرياض",          # ✅ مرئي
    "dateOfBirth": "1990-05-15"   # ✅ مرئي
  }
}
```

---

### 👥 3. Guest (ضيف - بدون تسجيل دخول)

**الوصول العام فقط:**

```
✅ يمكنه:
├─ عرض دليل الموظفين العام
└─ عرض بيانات عام لأي موظف

❌ لا يمكنه:
├─ تسجيل الدخول (لا توجد registration)
├─ عرض بيانات شخصية
├─ إنشاء أي شيء
└─ تعديل أو حذف

✅ API Endpoints:
├─ GET /api/employees/directory  (دليل الموظفين)
└─ GET /api/employees/:id        (موظف واحد)
```

**مثال: Guest يعرض دليل الموظفين**
```bash
curl -X GET http://localhost:5000/api/employees/directory

# الرد (بدون بيانات شخصية):
{
  "success": true,
  "data": [
    {
      "firstName": "أحمد",
      "lastName": "محمد",
      "position": "engineer",
      "department": "إنشاءات",
      "phoneNumber": "0501234567",
      "skills": ["AutoCAD"]
      # ❌ لا ssn, salary, address
    }
  ]
}
```

---

## هيكل قاعدة البيانات

### جداول (Collections)

#### 1. Users Collection

```typescript
{
  _id: ObjectId,
  name: String,                    // اسم كامل
  email: String,                   // فريد (مشفر في المرسل)
  password: String,                // مشفر بـ bcryptjs
  role: 'admin' | 'employee',      // الدور الأساسي
  createdAt: Date,
  updatedAt: Date
}
```

**مثال في MongoDB:**
```json
{
  "_id": ObjectId("507f1f77bcf86cd799439011"),
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "password": "$2a$12$encrypted...",
  "role": "employee",
  "createdAt": ISODate("2024-01-15"),
  "updatedAt": ISODate("2024-02-17")
}
```

---

#### 2. Employees Collection

```typescript
{
  _id: ObjectId,
  
  // Link إلى User
  user: ObjectId,              // Reference to User
  
  // معلومات أساسية
  firstName: String,
  lastName: String,
  phoneNumber: String,
  
  // معلومات العمل
  employeeId: String,          // معرّف فريد (EMP001)
  position: String,            // engineer, technician, supervisor, manager
  department: String,          // الجهة/القسم
  hireDate: Date,              // تاريخ التعيين
  
  // بيانات شخصية (select: false)
  ssn: String,                 // رقم التأمين الاجتماعي
  dateOfBirth: Date,
  address: String,
  emergencyContact: String,
  
  // بيانات العمل
  projects: [ObjectId],        // مصفوفة معرفات المشاريع
  skills: [String],
  salary: Number,              // (محمي من الاختيار)
  
  // الحالة
  isActive: Boolean,           // نشط أم لا
  joinDate: Date,              // تاريخ الانضمام
  
  createdAt: Date,
  updatedAt: Date
}
```

**مثال:**
```json
{
  "_id": ObjectId("507f1f77bcf86cd799439012"),
  "user": ObjectId("507f1f77bcf86cd799439011"),
  "firstName": "أحمد",
  "lastName": "محمد",
  "phoneNumber": "0501234567",
  "employeeId": "EMP001",
  "position": "engineer",
  "department": "إنشاءات",
  "hireDate": ISODate("2024-01-15"),
  "ssn": "123456789",  // select: false
  "salary": 5000,      // select: false
  "projects": [
    ObjectId("607f1f77bcf86cd799439013"),
    ObjectId("607f1f77bcf86cd799439014")
  ],
  "skills": ["AutoCAD", "Revit"],
  "isActive": true,
  "joinDate": ISODate("2024-01-15")
}
```

---

## API Endpoints

### 🔓 Endpoints عامة (Public)

#### 1. عرض دليل الموظفين

```http
GET /api/employees/directory

Response: 200 OK
{
  "success": true,
  "data": [
    {
      "_id": "emp123",
      "firstName": "أحمد",
      "lastName": "محمد",
      "position": "engineer",
      "department": "إنشاءات",
      "phoneNumber": "0501234567",
      "skills": ["AutoCAD"],
      "projects": []
    }
  ],
  "count": 45
}
```

**Authentication:** ❌ غير مطلوبة  
**المسموح**: الجميع  
**البيانات المستبعدة**: ssn, salary, address, dateOfBirth

---

#### 2. عرض موظف واحد

```http
GET /api/employees/:id

Response: 200 OK
{
  "success": true,
  "data": {
    "_id": "emp123",
    "firstName": "أحمد",
    "position": "engineer",
    "phoneNumber": "0501234567"
  }
}
```

**Authentication:** ❌ غير مطلوبة  
**المسموح**: الجميع  
**البيانات المستبعدة**: نفس البيانات العامة

---

### 🔐 Endpoints محمية (Protected)

#### 3. عرض الملف الشخصي

```http
GET /api/employees/me
Authorization: Bearer <token>

Response: 200 OK
{
  "success": true,
  "data": {
    "firstName": "أحمد",
    "lastName": "محمد",
    "ssn": "123456789",           ✅ مرئي
    "salary": 5000,               ✅ مرئي
    "dateOfBirth": "1990-05-15",  ✅ مرئي
    "address": "الرياض",          ✅ مرئي
    "emergencyContact": "..."     ✅ مرئي
  }
}
```

**Authentication:** ✅ مطلوبة  
**المسموح**: الموظف نفسه و Admin  
**البيانات**: جميع البيانات الشخصية

---

### 👨‍💼 Endpoints للـ Admin فقط

#### 4. إنشاء موظف

```http
POST /api/employees
Authorization: Bearer <admin_token>
Content-Type: application/json

Request:
{
  "firstName": "محمد",
  "lastName": "علي",
  "email": "mohammad@example.com",
  "password": "SecurePass123!",
  "phoneNumber": "0505555555",
  "employeeId": "EMP002",
  "position": "supervisor",
  "department": "إشراف",
  "hireDate": "2024-03-01",
  "skills": ["Management"],
  "salary": 4500
}

Response: 201 Created
{
  "success": true,
  "data": {
    "_id": "emp456",
    "firstName": "محمد",
    "employeeId": "EMP002",
    "user": {
      "_id": "user456",
      "email": "mohammad@example.com",
      "role": "employee"
    }
  },
  "message": "Employee created successfully with login credentials"
}
```

**ما يحدث**:
1. ✅ Admin يرسل طلب إنشاء
2. ✅ يتم التحقق من التفويض (Admin فقط)
3. ✅ يتم إنشاء User account تلقائياً
4. ✅ يتم إنشاء Employee profile
5. ✅ يتم تشفير كلمة المرور
6. ✅ يتم ربط المستخدم بالموظف
7. ✅ الموظف يمكنه تسجيل الدخول الآن

---

#### 5. تعديل موظف

```http
PUT /api/employees/:id
Authorization: Bearer <admin_token>

Request:
{
  "position": "manager",
  "department": "إدارة",
  "salary": 6000,
  "skills": ["Leadership", "Management"]
}

Response: 200 OK
{
  "success": true,
  "data": {
    "_id": "emp123",
    "position": "manager",
    "department": "إدارة",
    "salary": 6000
  },
  "message": "Employee updated successfully"
}
```

**القيود**:
- ❌ لا يمكن تغيير الـ user ID
- ❌ لا يمكن تغيير الـ employeeId
- ✅ يمكن تغيير أي حقل آخر

---

#### 6. إسناد مشاريع

```http
PUT /api/employees/:id/projects
Authorization: Bearer <admin_token>

Request:
{
  "projectIds": ["proj1", "proj2", "proj3"]
}

Response: 200 OK
{
  "success": true,
  "data": {
    "_id": "emp123",
    "projects": [
      {"_id": "proj1", "title": "مشروع فيلا"},
      {"_id": "proj2", "title": "مشروع برج"}
    ]
  },
  "message": "Employee projects updated successfully"
}
```

---

#### 7. حذف موظف

```http
DELETE /api/employees/:id
Authorization: Bearer <admin_token>

Response: 200 OK
{
  "success": true,
  "message": "Employee and associated user account deleted successfully"
}
```

**ما يحدث**:
1. ✅ التحقق من التفويض (Admin فقط)
2. ✅ حذف حساب المستخدم (User)
3. ✅ حذف ملف الموظف (Employee)
4. ✅ جميع البيانات المرتبطة حذفت

---

## Workflow العملية

### Workflow 1: Admin ينشئ موظف

```
┌─────────────────────────────────────────────┐
│ 1. Admin يفتح صفحة إنشاء موظف              │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. يملأ النموذج ويرسل البيانات             │
│    POST /api/employees + adminToken        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. السيرفر يتحقق:                          │
│    ├─ هل المستخدم admin? (YES)             │
│    ├─ هل البريد مكرر? (NO)                 │
│    └─ هل معرف الموظف مكرر? (NO)           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. ينشئ User account:                      │
│    ├─ Email: ahmed@example.com             │
│    ├─ Password: مشفرة                       │
│    └─ Role: employee                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. ينشئ Employee profile:                  │
│    ├─ firstName: أحمد                       │
│    ├─ position: engineer                   │
│    └─ user: Link to User account           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. الرد مع رسالة نجاح                       │
│    "Employee created with login credentials"│
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. الموظف يمكنه:                           │
│    ├─ تسجيل الدخول بـ:                     │
│    │  - Email: ahmed@example.com           │
│    │  - Password: كلمة المرور              │
│    └─ عرض ملفه الشخصي                      │
└─────────────────────────────────────────────┘
```

---

### Workflow 2: Employee يعرض ملفه الشخصي

```
┌─────────────────────────────────────────────┐
│ 1. الموظف يدخل الموقع                      │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 2. يدخل بيانات الدخول:                     │
│    - Email: ahmed@example.com               │
│    - Password: كلمته                        │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 3. POST /api/auth/login                    │
│    السيرفر:                                 │
│    ├─ يجد User بـ email                    │
│    ├─ يقارن password                      │
│    └─ ينشئ JWT Token                      │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 4. الموظف يحفظ Token في localStorage       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 5. يطلب صفحة ملفه الشخصي                    │
│    GET /api/employees/me + Token           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 6. السيرفر يتحقق:                          │
│    ├─ هل Token صحيح? (YES)                 │
│    ├─ هل User موجود? (YES)                │
│    └─ هل Employee موجود? (YES)            │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ 7. يرجع البيانات الشخصية:                   │
│    ├─ Name, Email, Position                │
│    ├─ Salary: 5000 ✅ (مرئي)               │
│    ├─ SSN: 123456789 ✅ (مرئي)             │
│    └─ Address: الرياض ✅ (مرئي)            │
└─────────────────────────────────────────────┘
```

---

## معالجة الأخطاء

### 400 - Bad Request

```json
{
  "success": false,
  "error": "Missing required fields."
}
```

**السبب**: بيانات ناقصة أو خاطئة  
**الحل**: تحقق من جميع الحقول المطلوبة

---

### 401 - Unauthorized

```json
{
  "success": false,
  "error": "Access denied. No token provided."
}
```

**السبب**: لا يوجد token أو token خاطئ  
**الحل**: تسجيل الدخول أولاً والحصول على token

---

### 403 - Forbidden

```json
{
  "success": false,
  "error": "You do not have permission to perform this action."
}
```

**السبب**: الدور غير كافي (موظف يحاول إنشاء)  
**الحل**: استخدم حساب admin

---

### 404 - Not Found

```json
{
  "success": false,
  "error": "Employee not found."
}
```

**السبب**: معرف الموظف غير موجود  
**الحل**: تحقق من الـ ID

---

### 409 - Conflict

```json
{
  "success": false,
  "error": "Email already registered."
}
```

**السبب**: البريد مستخدم بالفعل  
**الحل**: استخدم بريد مختلف

---

## 🧪 الاختبار

تم كتابة اختبارات شاملة:

```bash
# تشغيل جميع الاختبارات
npm test

# تشغيل مع coverage
npm test -- --coverage
```

**النسب المتوقعة**:
- ✅ 130+ test cases
- ✅ 95%+ code coverage
- ✅ جميع الـ workflows مختبرة
- ✅ جميع الأخطاء مختبرة

---

**آخر تحديث**: February 17, 2026  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للإنتاج
